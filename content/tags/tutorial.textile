----- 
:type: page
:permalink: tutorial
:title: "Tag: tutorial"
:filters_pre: 
- erb
- redcloth
-----

4 items are tagged with _tutorial_:

<% @site.pages.select{|p| p.attributes[:tags] && p.attributes[:tags].include?('tutorial')}.sort{|a,b| a.attributes[:date] <=> b.attributes[:date]}.reverse.each do |pg|
dir = (pg.attributes[:type] == 'page') ? '' : '/articles'
%>* <span class="<%= pg.attributes[:type] %>_link"> <a href="<%= dir %>/<%= pg.attributes[:permalink] %>/"><%= pg.attributes[:title] %></a></span>
<% end %>
		
